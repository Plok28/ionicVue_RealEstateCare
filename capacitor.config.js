import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicVue_RealEstateCare',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
